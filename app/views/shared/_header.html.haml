.jumbotron
  .container
    %p Next Up Chicago

.container
  .navbar
    .navbar-inner
      .nav
        %li{class: (current_page?(root_path) ? "active" : "")}
          = link_to "Home", root_path
        %li{class: (current_page?(properties_path) ? "active" : "")}
          = link_to "Properties", properties_path
        %li{class: (current_page?(guides_path) ? "active" : "")}
          = link_to "Guides", guides_path
        %li{class: (current_page?(about_us_path) ? "active" : "")}
          = link_to "About Us", about_us_path
        %li{class: (current_page?(contact_index_path) ? "active" : "")}
          = link_to "Contact Us", contact_index_path
